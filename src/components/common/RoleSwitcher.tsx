import React from 'react';

/**
 * RoleSwitcher:
 * Removed from the public customer UI per the 2026 production redesign.
 * Internal admin/kitchen/corporate dashboards remain accessible via direct routes & authentication.
 */
export const RoleSwitcher: React.FC = () => {
  return null;
};
